import React from "react";
import "./LastColumn.css";

function LastCol({ result }) {
  return (
    <div id="last-col">
      <div id="show-result">{result}</div>
    </div>
  );
}

export default LastCol;
